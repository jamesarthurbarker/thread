# Thread — update feed

This repository hosts the [Sparkle](https://sparkle-project.org) auto-update feed for the Thread macOS app.

- `updates/appcast.xml` — the update manifest Thread checks
- `updates/Thread-*.dmg` — notarized release builds

Installer/download for the latest version: `updates/`.
